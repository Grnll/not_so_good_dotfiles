-- Edit config.h in dwm's PKGBUILD directory (generally /var/abs/community/dwm/)
-- and rebuild package if you want to change settings.
-- Or, place custom config.h in $SRCDEST/dwm-git/config.h and rebuild.
